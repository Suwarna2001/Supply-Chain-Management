# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~
GET     /                  controllers.HomeController.index()
POST    /uploadJson       controllers.OrderController.uploadJson
GET     /kafkaMessages     controllers.KafkaController.index
